<?xml version="1.0" encoding="UTF-8"?>
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出-->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数-->
<configuration monitorInterval="5">
    <!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->

    <!--变量配置-->
    <Properties>
        <!-- 格式化输出：%clr：带颜色输出；%date：日期；%thread：线程名；%X{LOG_ID}：日志ID；%-5level：级别从左显示5个字符宽度；%msg：日志消息；%n：换行符 -->
        <property name="LOG_PATTERN"
                  value="%clr{[%date{HH:mm:ss.SSS}]}{yellow} %clr{[%thread]}{blue} %clr{[%X{LOG_ID}]}{magenta} %clr{[%-5level]} %clr{[%c{1.}]}{cyan} [%line] - %msg%n"/>
        <!-- 定义日志存储的路径 -->
        <property name="FILE_PATH" value="/data/log/"/>
    </Properties>

    <appenders>
        <console name="Console" target="SYSTEM_OUT">
            <!--输出日志的格式-->
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <!--控制台只输出level及其以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="debug" onMatch="ACCEPT" onMismatch="DENY"/>
        </console>
    </appenders>

    <loggers>
        <!-- mybatis 控制台日志 -->
        <logger name="org.mybatis" level="debug">
            <AppenderRef ref="Console"/>
        </logger>
        <!-- redisson 控制台日志 -->
        <logger name="org.redisson" level="info">
            <AppenderRef ref="Console"/>
        </logger>
        <!-- springframework 控制台日志 -->
        <Logger name="org.springframework" level="info">
            <AppenderRef ref="Console"/>
        </Logger>

        <root level="debug">
            <appender-ref ref="Console"/>
        </root>
    </loggers>

</configuration>
